name: poisson_warp
description: A sample command-line application.
version: 1.0.0
repository: https://github.com/LiveUser/poisson_warp

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  big_dec: ^0.0.8+8
  
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
